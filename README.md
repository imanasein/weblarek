Ссылка на репозиторий https://github.com/imanasein/weblarek.git

# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

##### Данные
Графическая схема интерфейсов данных и моделей данных, а также их структурное взаимодействие размещена в корне проэкта название файла схемы: ./DataSсheme.png

##### Интерфейс IProduct
Интерфейс описывающий форму объектов каждого товара магазина.

Свойства интерфейса:  
`id: string` - уникальный идентификатор id товара,
`description: string` - краткое описание товара,
`image: string` - путь к изображению товара для отображения,
`title: string` - название товара,
`category: string` - категория товара,
`price: number | null` - стоимость товара (у некторых товаров стоимость может отсутствовать).

##### Интерфейс IBuyer
Интерфейс описывающий форму объекта данных покупателя.

Свойства интерфейса:  
`payment: TPayment` - типа оплаты покупателем (карта, наличные или пустое - незаполненное поле ввода),
`email: string` - e-mail адресс покупателя, 
`phone: string` - контактный телефон покупателя,
`address: string` - адресс доставки товара, указанный покупателем.

##### Тип данных TPayment
Литеральный тип данных, использующийся для описания выбранного способа оплаты покупателем, использует три фиксированных типов оплаты.

Фиксироанные значения:
`"card": string` - выбирается покупателем тип оплаты картой онлайн,
`"cash": string` - выбирается покупателем тип оплаты наличными при получении товара,
`"":string` - пустое поле значение по умолчанию, когда покупатель ещё не выбрал ни один из типов.

##### Тип данных TBuyerErrors
Тип данных описывающий объект вида "ключ" : "значеине", исполбзуемый при валидации данных покупателя, во время оформления заказа товаров.

Конструктор:
`Partial<Record<keyof IBuyer, string>>` создаёт объектный тип ключами котрого являются ключи интерфейса IBuyer ('payment' | 'email' | 'phone' | 'address'), а значения имеют строковый тип и содержат тектовое сообщение об ошибке в конкретном поле покупателя, так же все поля объекта являются необязательными.

##### Модели Данных
Предназначен для хранения и работу с полученными данными с сервера. Формируются из массива товаров путем взамидействия покупателя с интерфейсом: каталог товаров, карзина со списком выбранных товаров, а также данные о покупателе, которые он заполняет сам при заказе.

##### Класс Catalog
Основной класс описывающий массив товаров, которые можно купить в магазине (массива объектов).

Конструктор:
`constructor(initialProducts: IProduct[] = [])` В конструктор передаётся массив товаров, доступных для покупки.

Поля класса:
`productCatalog: IProduct[] = []` - хранит массив всех товаров,
`selectedProduct: IProduct | null = null;` - выбранный покупателем товар из каталога товаров, для более дитального отображения на главной странице, для расширенного ознакомления покупателя с потребительскими качествами товара.

Методы: 
`get products(): IProduct[] ` - получение массива товаров из модели;
`getItem(id: string): IProduct | null`  - получение одного товара по его уникальному id;
`setSelectedItem(product: IProduct): void` - сохранение товара для подробного отображения;
`getSelectedItem(): IProduct | null` - получение товара, выбранный для подробного отображения.

##### Класс Cart
Основной класс для хранения массива товаров, которые покупатель выбрал для покупки.

Конструктор класса не принимает параметров.

Поля класса:
`cartList: IProduct[]` - хранит массив товаров, выбранных покупателем для покупки, по умолчанию пустой массив.

Методы: 
`getCartList: IProduct[]` - получение массива товаров, которые находятся в корзине,
`addItem(product: IProduct): void`  - добавление товара, который был получен в параметре, в массив корзины,
`removeItem(product: IProduct): void` - удаление товара, полученного в параметре из массива корзины,
`deleteAll(): void` - очистка корзины,
`getTotalPrice(): number` - получение стоимости всех товаров в корзине,
`getItemsQuantity(): number` - получение количества товаров в корзине,
`isItemInCart(id: string): boolean ` - проверка наличия товара в корзине по его id, полученного в параметр метода.

##### Класс Buyer
Основной класс описывающий данные покупателя, которые он должен указать при оформлении заказа, реализующий интерфейс IBuyer.

Конструктор не принимает параметров.

Поля класса:  
`buyerPayment: TPayment` - класс описывающий выбранный тип оплаты покупателем (карта, наличные или пустое - незаполненное поле ввода),
`buyerEmail: string` - e-mail адресс покупателя,
`buyerPhone: string` - контактный телефон покупателя,
`buyerAddress: string` - адресс доставки товара, указанный покупателем.

Методы: 
`updateBuyerData(data: Partial<Buyer>): void` сохранение данных email,телефона,адреса и типа оплаты покупателя в модели, предусмотрено, что данные покупателя можно сохратять по отдельности или все разом,
`getBuyer(): IBuyer` получение всех данных покупателя,
`clearBuyerData(): void` очистка всех данных покупателя,
`validate(): TBuyerErrors` валидация данных покупателя, возвращает объект "поле" : "ошибка" ключами которого являются поля класса Ibuyer (payment, email, phone,address), а значеиями текстовое сообщение о выявленных ошибках при заполнении соответствующих полей.

###### Слой коммуникации

###### Класс Communication
Основной класс отвечающий за коммуникацию с сервером, использующий композицию классов (используются методы "get" и "post" класса "Api"), для получения данных с сервера (списка товаров) и отправку данных заказа на сервер.

Конструктор:
`constructor(api: IApi)` В конструктор передаётся объект, соответствующий интерфейсу IApi.

Методы: 
`async getProducts(): Promise<ProductsResponse>` - получает список товаров с сервера, возвращаемое значение - промис содержащий объект "ProductsResponse" (включающий: массив товаров, предложенных к продаже и общее количество предложенных товаров).
`async createOrder(orderData: OrderData): Promise<OrderResponse>` - Отправляет данные заказа на сервер и получает подтверждение, параметр "OrderData" — объект с данными заказа (включающий: информацию о покупателе (IBuyer), список ID товаров и итоговую сумму заказа). Возвращаемое значение - промис содержащий объект "OrderResponse" (влючающий в себя информацию уникальный идентификатор ID заказа и итоговую стоимость заказа).

###### Слой Визуального Представления (View)
Слой приложения предназначенный для отобрадения модели через Презентер,на элементы с которыми взаимодействует пользователь добавлены эмитеры событий,(клики по кнопкам, чекбоксам и ввод текста). Каждый класс Представления отвечает за свой блок в HTML разметке.

###### Класс Component
Базовый родительский абстрактный класс для всего слоя Предсавления, является дженериком.

Конструктор:
`protected constructor(protected readonly container: HTMLElement)` - доступный только в дочерних классах конструктор передаётся контейнер - HTML-элемент разметки за отображение которого отвечает класс.

Поля класса:
`protected readonly container: HTMLElement` - доступный только для чтения в дочерних классах хранит HTML-элемент разметки за отображение которого отвечает класс.

Методы: 
`protected setImage(element: HTMLImageElement, src: string, alt?: string)` - сеттер - доступный только в дочерних классах для установки изображения в HTMLImage-элементе, в качестве аргументов в медот передаются: element - HTMLImage-элементе, src - ссылка на рисунок в виде строки и опционально в виде строки описание кортинки которое заносится в атрибут alt указанного HTMLImage-элемента

`render(data?: Partial<T>): HTMLElement` - метод возвращающий HTML-элемент разметки принимающий обционально данные и дженерик интерфейса дочернего элемента для отсечения лишних данных, в качестве интерфейса в дочерних классах, для корректной работы метода, указываются поля -соотвествующие сеттерам дочернего класса и тот же тип данных, которые принимает соответствующий сеттер. При вызове метода без переданных данных возвращает HTML-элемент без изменения данных.

###### Класс Header
Основной класс отвечающий за отображение элементов в шапке(header) сайта, кнопки с изображением карзины и счетчиком на ней количества товаров, находящихся в карзине.

Интерфейс, используемый классом:
`interface IHeader {`
    `counter: number;`
`}`

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` В конструктор передаётся HTML-элемент <header> и экземпляр брокера событий events.

Поля класса:
`protected buttonBasket: HTMLButtonElement` - защищенное поле HTML-элемент кнопки карзины, 
`protected counterElement!: HTMLElement` - защищенное поле HTML-элемент счетчика элементов в корзине, 
`protected events: IEvents;` - защищенное поле хранит экземпляр брокера событий events.

Методы: 
`set counter(value: number)` - сеттер для установки содержимого счётчика товаров в карзине;

###### Класс Gallery
Основной класс отвечающий за отображение каталога товаров, доступных пользователю для покупки;

Интерфейс, используемый классом:
`interface IGallery {`
    `catalog: HTMLElement[];`
`}`
Конструктор:
`constructor(container: HTMLElement) ` В конструктор передаётся HTML-элемент в котором находится галлерея для отображения товаров.

Поля класса:
`protected catalogElement: HTMLElement` - защищенное поле HTML-элемент галереи, 

Методы: 
`set catalog(items: HTMLElement[])` - сеттер для установки массива карточек товаров;

###### Класс Modal
Основной класс отвечающий за отображение модального окна, в которое передаются элементы для отображения: карзина с товарами, формы для заполнения данных о покупателе, карточка товара с подробным описанием, а также сообщение об успешном заказе.

Интерфейс, используемый классом:
`interface IModal {`
    `content: HTMLElement;`
`}`

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` В конструктор передаётся HTML-элемент в котором находится модальное окно и экземпляр брокера событий events.

Поля класса:
`protected closeButton: HTMLButtonElement` - защищенное поле HTML-элемент кнопки закрытия модального окна,
`protected contentElement: HTMLElement` - защищенное поле HTML-элемент в который передается контент для отображения в модальном окне.

Методы: 
`set content(item: HTMLElement)` - сеттер для установки HTML-элемента для отображения в модальном окне,
`show(): void` - метод скрывающий модальное окно,
`hide(): void` - метод делающий модальное окно видимым пользователю.

###### Класс Basket
Основной класс отвечающий за отображение карзины с товарами в модальном окне.

Интерфейс, используемый классом:
`interface IBasket {`
    `orderPrice: number;`
    `basketList: HTMLElement[];`
    `buttonText: string;`
    `buttonStatus: boolean`
`}`

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` В конструктор передаётся HTML-элемент в котором находится корзина и экземпляр брокера событий events.

Поля класса:
`protected basketListElement: HTMLElement` - защищенное поле HTML-элемент контейнер в котором находятся карточки товаров карзины,
`protected orderButton: HTMLButtonElement` - защищенное поле HTML-элемент кнопки покупки товаров в корзине,
`protected totalPrice: HTMLElement` - защищенное поле HTML-элемент итоговой цены всех товаров, находящихся в корзине.

Методы: 
`set orderPrice(value: number)` - сеттер для установкитоговой цены всех товаров, находящихся в корзине, качестве переметра передается число,
`set basketList(items: HTMLElement[])` - сеттер для установки массива карточек в контейнер карзины, для карточек товаров.
`set buttonStatus(value: boolean)` - сеттер переключающий состояние кнопки покупки карзины, в качестве аргумета передыётся булево значение.

###### Класс Success
Основной класс отвечающий за отображение сообщение об успешном заказе в модальном окне

Интерфейс, используемый классом:
`interface ISuccess {`
    `totalPrice: number;`
`}`

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` В конструктор передаётся HTML-элемент в котором находится сообщение об успешном обработанном заказе с сервера для отображения в модальном окне.

Поля класса:
`protected orderTotal: HTMLElement` - защищенное поле HTML-элемент в который передаётся сообщение от сервера об итоговой стоимости, 
`protected closeButton: HTMLButtonElement` - защищенное поле HTML-элемент кнопки закрытия окно с ссобщением.

Методы: 
`set totalPrice(value: number)` - сеттер для установки итоговой стоимости заказанных товаров, в качестве аргумента передаётся число.

###### Класс Card
Основной родительский класс отвечающий за отображение товара, является общим родительским классом для всех карточек товаров.

Интерфейс, используемый классом:
`interface ICard {`
    `title: string;`
    `price: number | null;`
`}`

Конструктор:
`constructor(container: HTMLElement)` В конструктор передаётся HTML-элемент в котором находится карточка товара.

Поля класса:
`protected titleElement: HTMLElement` - защищенное поле HTML-элемент в который передаётся название товара, 
`protected priceElement: HTMLElement` - защищенное поле HTML-элемент в который передаётся стоимость товара.

Методы: 
`set title(value: string)` - сеттер для установки названия товара в поле, в качестве аргумента передаётся строка,
`set price(value: number | null)` - сеттер для установки стоимости товара в поле, в качестве аргумента передаётся число, но также предусмотрено, что некоторые товары могут не иметь стоимости, для этого предусмотрено значение - null.

###### Класс CardBasket
Основной класс отвечающий за отображение карточки товара в корзине, является дочерним классом Card.

Интерфейс, используемый классом:
`interface ICardBasket {`
    `index: number;`
`}`

Конструктор:
`constructor(container: HTMLElement, protected actions: ICardAction)` В конструктор передаётся HTML-элемент в котором находится карточка товара, и функция реализующая эмиссию события при клике на кнопку удалить товар, для последующей подписки на это событие.

Поля класса:
`private cardIndex: HTMLElement` - приватное поле (доступное только внутри класса) HTML-элемент в который передаётся порядковый номер товара в корзине, 
`private deleteButton: HTMLButtonElement` - приватное поле (доступное только внутри класса) HTML-элемент кнопки удаления товара из корзины.

Методы: 
`set index(value: number)` - сеттер для установки порядкового номера товара в корзине.

###### Класс CardGallery
Основной класс отвечающий за отображение карточки товара в галерее, является дочерним классом Card.

Интерфейс, используемый классом:
`interface ICardGallery {`
    `category: CategoryKey;`
    `image: string;`
`}`

Конструктор:
`constructor(container: HTMLElement, actions?: ICardAction)` - в конструктор передаётся HTML-элемент в котором находится карточка товара, и функция реализующая эмиссию события при клике на карточку товара, которая является кнопкой.

Поля класса:
`protected categoryElement: HTMLElement` - защищенное поле HTML-элемент категории товара,
`protected imageElement: HTMLImageElement` - защищенное поле HTML-элемент изображения товара,

Методы: 
`set category(value: CategoryKey)` - сеттер для установкитоговой категории товаров, в качестве аргуметна передаётся категория товара, реализовано изменение цвета ярлыка категории, в зависимости от названия (ключ: категория, значение: класс, который добавляется),
`set image(value: string)` - сеттер для установки изображения в поле, в качестве аргумента - строка, эндпоинт адреса рисунка, метод использует метод родительского класса Component.

###### Класс CardPriview
Основной класс отвечающий за отображение карточки выбранного товара в модальном окне, является дочерним классом Card.

Интерфейс, используемый классом:
`interface ICardPreview {`
    `category: CategoryKey;`
    `image: string;`
    `description: string;`
`}`

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents) ` - в конструктор передаётся HTML-элемент в котором находится карточка товара, и экземпляр брокера событий.

Поля класса:
`protected categoryElement: HTMLElement` - защищенное поле HTML-элемент категории товара,
`protected imageElement: HTMLImageElement` - защищенное поле HTML-элемент изображения товара,
`protected descriptionElement: HTMLElement` - защищенное поле HTML-элемент краткого описания товара,
`private buyButton: HTMLButtonElement` - защищенное поле HTML-элемент кнопки "Купить".

Методы: 
`set category(value: CategoryKey)` - сеттер для установкитоговой категории товаров, в качестве аргуметна передаётся категория товара, реализовано изменение цвета ярлыка категории, в зависимости от названия (ключ: категория, значение: класс, который добавляется),
`set image(value: string)` - сеттер для установки изображения в поле, в качестве аргумента - строка, эндпоинт адреса рисунка, метод использует метод родительского класса Component.
`set description(value: string)` - сеттер для установки краткого описания о товаре в поле, в качестве аргумента - строка, 
`set buttonText(value: string)` - сеттер для установки надписи на кнопке в элементе карточки, в качестве аргумента - строка, 
`set buttonStatus(value: boolean)` - сеттер для установки доступности для нажатия кнопки "Купит", в качестве аргумента - булево значение. 

###### Класс Form
Основной общий родительский класс отвечающий за отображение формы данных о покупателе в модальном окне.

Интерфейс, используемый классом:
`interface IForm {`
    `errors: string;`
    `valid: boolean;`
`}`

Конструктор:
`constructor(container: HTMLFormElement, protected events: IEvents)` - в конструктор передаётся HTML-элемент в котором находится форма и экземпляр класса брокера событий.

Поля класса:
`protected submitButton: HTMLButtonElement` - защищенное поле HTML-элемент кнопки type="submit" формы,
`protected errorElement: HTMLElement` - защищенное поле HTML-элемент ошибок валидации данных формы,

Методы: 
`set errors(value: string)` - сеттер для установкитоговой ошибок валидации данных формы, в качестве аргуметна передаётся строка,
`set valid(value: boolean)` - сеттер для установки доступности для нажатия кнопки type="submit" формы, в качестве аргумента - булево значение.

###### Класс FormOrder
Основной класс отвечающий за отображение формы адреса и типа платы в модальном окне, является дочерним классом Form. 

Интерфейс, используемый классом:
`interface IFormOrder {`
    `payment: TPayment;`
    `phone: string;`
`}`

Конструктор:
`constructor(container: HTMLFormElement, protected events: IEvents` - в конструктор передаётся HTML-элемент в котором находится форма, и экземпляр брокера событий.

Поля класса:
`protected buttonCard: HTMLButtonElement` - защищенное поле HTML-элемент кнопки выбора оплаты картой он-лайн,
`protected buttonCash: HTMLButtonElement` - защищенное поле HTML-элемент кнопки выбора оплаты наличными при получении товара,
`protected addressInput: HTMLInputElement` - защищенное поле HTML-элемент поля ввода адресса доставки товара покупателю.

Методы: 
`set buttonStatus(value: TPayment)` - Сеттеры для установки состояния кнопки, добавление соответствующего класса кнопке для её визуального выделения.
 
###### Класс FormContacts
Основной класс отвечающий за отображение формы вода email и телефона в модальном окне, является дочерним классом Form. 

Интерфейс, используемый классом:
`interface IFormContacts {`
    `email: string;`
    `image: string;`
    `description: string;`
`}`

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents) ` - в конструктор передаётся HTML-элемент в котором находится форма и экземпляр класса брокера событий.

Поля класса:
`protected emailInput: HTMLInputElement` - защищенное поле HTML-элемент поля ввода email адреса почты покупателя,
`protected phoneInput: HTMLInputElement` - защищенное поле HTML-элемент поля ввода телефона покупателя.

Методы: 
своих методов не имеет, использует методы родительского класса Form

###### События, генерируемые слоем Представления (View)

`galleryCard:selected` - Событие: карточка товара выбрана, срабатывает при клике мишью на кнопку- карточку товара в галерее,
`basket:open` - Событие: открыть карзину, срабатывает при клике мышью на кнопку карзины в heaher,
`product:to_cart` - Событие: товар купить/удалить, срабатывает при клике мышью на кнопку "Купить"/"Удалить из корзины" в карточке товара в модальном окне,
`product:delete` - Событие: товар удалить, срабатывает при клике мышью на кнопку "удалить товар из карзины" карточки товара в корзине,
`modal:close` - Событие: модальное окно - закрыть, срабатывает при клике мышью на кнопку в модальном окне "Закрыть",
`basket:order` - Событие: карзина заказать, срабатывает при клике мышью на кнопку "Оформить" в корзине,
`payment_card:selected` - Событие: конпка "Оплата онлайн", срабатывает при клике мышью на кнопку ""Оплата онлайн"" формы заполнения способа оплаты и адреса достовки товаров,
`payment_cash:selected` - Событие: конпка "При получении", срабатывает при клике мышью на кнопку "При получении" формы заполнения способа оплаты и адреса достовки товаров,
`address:changed` - Событие: поле ввода "Адрес доставки" заполнено, срабатывает при заполнения поля ввода "Адрес доставки" формы заполнения способа оплаты и адреса достовки товаров,
`email:changed` - Событие: данные поля ввода email изменены, срабатывает при заполнения поля ввода "Email" формы заполнения email адреса и телефона покупателя,
`phone:changed` - Событие: данные поля ввода телефона изменены, срабатывает при заполнения поля ввода "Телефон" формы заполнения email адреса и телефона покупателя,
`order:submit` - Событие нажатие кнопки "Далее", срабатывает при клике мышью на кнопку "Далее" формы заполнения способа оплаты и адреса достовки товаров,
`contacts:submit` - Событие нажатие кнопки "Оплатить", срабатывает при клике мышью на кнопку "Оплатить" формы заполнения email адреса и телефона покупателя,
`success:close` - Событие: Успешно, закрыть, срабатывает при кликом мышью на кнопку "За новыми покупками!" в модальном окне подтверждения успешной оплаты и отправки заказа на сервер


###### События, генерируемые слоем Моделей данных (Model)

`catalog:changed` - Событие: каталог изменился, генерируется моделью даннных каталога товаров "Catalog", при вызове сеттера "set products". 
`catalog:product_selected` - Событие: товар выбран для детального отображения,  генерируется моделью даннных каталога товаров "Catalog", при вызове сеттера "setSelectedItem".
`basket:changed` - Событие карзина: изменилась, генерируется моделью даннных корзины товаров "Cart", при вызове метода добавления товара в корзину "addItem" или метода удаления товара из карзины "removeItem" или метода удаления всех товаров из корзины "deleteAll".
`buyer:changed` - Событие: модель данных Buyer изменилась, генерируется моделью данных покупателя "Buyyer", при вызове метода добавления данных покупателя "updateBuyerData".

###### Слой Презентер
Презентер одностраничного сайта реализован в одном файле "main.ts" который обрабатывает события генерируемые слоем Представления и Моделями данных и реализует бизнес-логику работы приложения:
* * Главная страница * *
* каталог товаров загружается с сервера в модельданных каталога товаров;
* загруженные товары отрисовываются на главной странице в каталоге товаров;
* при нажатии на карточку товара открывается модальное окно с информацией о товаре;
* при нажатии на иконку корзины открывается корзина;
* на иконке корзины отображается счётчик — он показывает количество товаров в корзине;
* * Просмотр товара * *
* показана детальная информация о товаре;
* присутствует кнопка «Купить», если товар не был выбран покупателем;
* при нажатии на кнопку «Купить» товар добавляется в корзину (если не был добавлен в корзину ранее);
* если товар находится в корзине, кнопка должна быть заменена на «Удалить из корзины»;
* при нажатии на кнопку «Удалить из корзины» товар удаляется из корзины;
* после нажатия кнопки модальное окно закрывается;
* если у товара нет цены, кнопка в карточке должна быть заблокирована и иметь название «Недоступно».
* * Корзина * *
* отображается список товаров, которые выбрал покупатель;
* для каждого товара указана цена и присутствует кнопка удаления. Если нажать на кнопку, товар удаляется из корзины;
* отображается общая стоимость товаров в корзине;
* присутствует кнопка оформления покупки;
* если в корзине нет товаров, кнопка оформления должна быть деактивирована;
* если товаров нет в корзине — вместо списка товаров выводится надпись «Корзина пуста»;
* при нажатии на кнопку «Оформить» открывается модальное окно с формой оформления товара.
* * Оформление товара * *
* (Первый шаг)
* выбор способа оплаты;
* ввод адреса доставки;
* если адрес доставки не введён, появляется сообщение об ошибке;
* если способ оплаты не выбран, появляется сообщение об ошибке;
* кнопка «Далее» может быть активной, если: на форме нет ошибок, выбран способ оплаты и поле адреса доставки непустое;
* при нажатии «Далее» открывается вторая форма оформления покупки.
* (Второй шаг)
* ввод почты и телефона покупателя;
* если одно из полей не заполнено, появляется сообщение об ошибке;
* кнопка «Оплатить» может быть активной, только если на форме нет ошибок и поля почты и телефона непустые;
* при нажатии на кнопку оплаты выполняется передача данных о заказе на сервер, появляется сообщение об успешной оплате, товары удаляются из корзины, данные покупателя очищаются.
* * Модальные окна закрываются:
* по клику вне модального окна;
* по клику на иконку «Закрыть» (крестик)